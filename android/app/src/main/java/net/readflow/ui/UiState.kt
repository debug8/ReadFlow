package net.readflow.ui

/**
 * Стан головного екрана — один незмінний знімок.
 *
 * Усе, що бачить користувач, приходить звідси й нізвідки більше: так стан
 * переживає поворот екрана й не розповзається по компонентах. Нові поля
 * додаються з наступними задачами (статистика, таймер, режими), і кожне
 * має значення за замовчуванням, щоб `UiState()` завжди був валідним.
 */
data class UiState(
    /** Текст, який ввів або вставив учитель. Порожній — стан «нічого не введено». */
    val text: String = ""
)
